Frontend (Next.js) for Accident Risk

Run locally:

cd <your_project_path>/ai-ml-demo/c3/frontend
npm install
npm run dev

The frontend will start on http://localhost:3000 and calls the backend at http://localhost:8000/api/predict_risk. Ensure backend is running and accessible from the host machine.
